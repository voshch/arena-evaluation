# control: stock scenario mode on the same route
stages:
- name: beckon_checkpoint_scenariomode
  map: hospital_1_bare
  robot: jackal
  tm_robots: scenario
  tm_obstacles: scenario
  episodes: 1
  seed: 77000
  timeout: 120s
  config:
    scenario:
      file: beckon_checkpoint
      gesture_mode: disabled
    patrol:
      service_agent: caller
      clip: beckon
      service_radius: 2.5
      service_dwell: 0.0
